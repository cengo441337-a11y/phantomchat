<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network: required to reach IPFS gateways and (later) relay nodes. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- App-lock: biometric unlock via local_auth. USE_FINGERPRINT (deprecated
         API 28) is intentionally NOT declared — USE_BIOMETRIC supersedes it. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- "Add contact via QR" scanner — see lib/screens/qr_scan.dart. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Background-delivery: lets the user opt out of OEM battery optimisation
         so the relay/connection-keeper survives Doze on Xiaomi/Huawei/Samsung. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Wave 11B: voice messages — recording on the chat screen. The
         `record` plugin uses MediaRecorder (or MediaCodec for opus) which
         requires this runtime permission on API 23+. We request it
         lazily on first mic press, not at install time. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Wave 8B: background relay-listener foreground service -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Wave 8B: PhantomChat relay-listener foreground service -->
        <service
            android:name=".RelayForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />
        <!-- Wave 8B: opt-in auto-start on device boot -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
